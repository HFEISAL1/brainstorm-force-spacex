# Brainstorm Force Interview Assessment

## API Documentation

To set up the DB, run `mysql -uroot -p < initdb.sql`, See `initdb_example.sql`
To initialize users database table for auth flow, run `php dbseed.php`
